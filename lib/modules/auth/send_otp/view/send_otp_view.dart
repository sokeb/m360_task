import 'package:flutter/material.dart';

class SendOtpView extends StatelessWidget {
  const SendOtpView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
      child: Scaffold(
        body: Center(
          child: Text('Send OTP View'),
        ),
      ),
    );
  }
}
